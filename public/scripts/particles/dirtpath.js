class Dirtpath{
    playEffect(parctx, src, index){
        let img = document.getElementById(src);
        // parctx.globalAlpha = 1-index/100
        // parctx.drawImage(img, objectList["character"].x, objectList["character"].y-objectList["character"].h-index, this.w, this.h);
        // parctx.globalAlpha = 1
  }
}